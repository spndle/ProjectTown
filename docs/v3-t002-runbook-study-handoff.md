# T002 delivery-verification runbook Study handoff

Only create a new Study after the User supplies a new unique Study ID, sibling work root,
participant protocol and manual baseline. Use candidate profile `projecttown-human-pdf-v4`
with manifest v4. Generate a new v3 Result/PDF in that new root; never reuse frozen evidence.

The participant must separately record disposition, structural rewrite, citation usability,
control rating, improvement reason, actions and active elapsed time. Historical evidence is
read-only. A Summary remains awaiting User acceptance and does not authorize Apply/Publish.
